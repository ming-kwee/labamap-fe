'use client';

// Basic Info / Product Information section — the generic collapsible fields section.
// Section identity is resolved from sectionKey at render time (see FieldsSection).
export { default } from './_shared/FieldsSection';
