'use client';

// Pricing & Inventory section — the generic collapsible fields section.
// Price/quantity masking is handled per-field by FieldRenderer (classifyNumericField).
export { default } from './_shared/FieldsSection';
