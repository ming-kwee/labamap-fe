import PlaygroundClient from "./_components/PlaygroundClient";

export const metadata = { title: "Post-Processing Playground | Platform Admin" };

export default function Page() {
  return <PlaygroundClient />;
}
