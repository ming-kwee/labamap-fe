import PublishDiagnosticsPage from "@/modules/ai-admin/components/diagnostics/PublishDiagnosticsPage";

export const metadata = { title: "Publish Diagnostics | Platform Admin" };

export default function Page() {
  return <PublishDiagnosticsPage />;
}
