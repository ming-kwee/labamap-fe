import { InventorySyncTable } from "@/modules/channel-platform/components/InventorySyncTable";

export const metadata = { title: "Channel Platform | Inventory" };

export default function ChannelInventoryPage() {
  return <InventorySyncTable />;
}
