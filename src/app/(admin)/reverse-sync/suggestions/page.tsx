import SuggestionsInboxPage from "@/modules/reverse-sync/components/SuggestionsInboxPage";

export const metadata = { title: "Suggestions from Channel" };

export default function Page() {
  return <SuggestionsInboxPage />;
}
