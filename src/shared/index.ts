// Main barrel export for shared resources.
// Only ./contexts currently ships an index; ui/hooks/services/utils/types have no barrel yet
// (import them directly, e.g. "@/shared/ui/icons/Icons"). Re-add here once those expose an index.
export * from './contexts';
